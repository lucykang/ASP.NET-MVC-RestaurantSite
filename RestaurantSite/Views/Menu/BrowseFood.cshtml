@using RestaurantSite.Models
@model Food

@{
    ViewBag.Title = "BrowseFood";
}

<h2>@Model.Name</h2>
<ul>
    @foreach (var item in Model.FoodItems)
    {
        <li>
            @Html.ActionLink(item.Name, "FoodDetails", new { id = item.ItemID })
        </li>
    }
</ul>